# prompt-library · 优质提示词库 / 风格库

> 本目录存放**高质量、可直接复刻的成品提示词与配套资料**，按「工作流/模型」分类管理，方便日后"翻到就复刻"。
>
> **与工作流侧的分工（勿混）**：`workflows/<模型>/prompting.md` 管「**书写格式规范**」、`guide.md` 管「**工作流使用指南**」——那边是"**怎么写、怎么提交**"；本目录是"**写好的成品长什么样、怎么照着复刻**"。

## 一、分类机制（重要：以后一律按此归置，禁止乱放）

顶层按**模型风格库**组织，保持架构受控成长：

### 模型风格库 —— `<模型>/`
每个模型一个子目录，存放该模型专属的优质提示词风格库。

```
prompt-library/
├── krea2/          # krea2 风格库（见其 README：一个风格一份 NN_风格.md）
├── anima/          # anima 风格库（见其 README）
└── ...
```
- 每个 `<模型>/` 目录自带 `README.md`，写明**该模型的分类机制 + 风格索引**。
- 风格文档命名 `NN_风格名.md`（两位序号前缀 + 中文风格名）。

## 二、当前内容

| 目录 | 类型 | 内容 |
|---|---|---|
| `krea2/` | 模型风格库 | 01 巨构指导学 / 02 设计海报 / 03 人设卡片 / 04 旅行海报卡片（详见 `krea2/README.md`） |
| `anima/` | 模型风格库 | 01 杂志封面贴纸网点海报（详见 `anima/README.md`） |

## 三、归置规则（禁止乱放）

1. **模型专属风格** → 放对应 `<模型>/` 子目录，一个风格一份 `NN_风格.md`，并更新该目录 README 索引。
2. **书写格式 / 工作流使用指南** → **不放这里**，归 `workflows/<模型>/prompting.md` 与 `guide.md`。
3. 新增顶层目录前，先在本 README 登记，保持架构受控成长。
4. 负面提示词一律不入库（内嵌工作流，不交付）。

## 四、与工作流侧的关系

- 书写方法论 / 格式：`workflows/krea2/prompting.md`、`workflows/krea2/guide.md`
- 成品范式与复刻资料：**本目录**
- 执行：`execution/EXECUTION.md`

## 五、来源

- `krea2/`：作者 2026-09 提供与产出的 krea2 优质提示词（巨构 / 设计海报 / 人设卡片 / 旅行海报卡片）
- `anima/`：作者 2026-09 产出的 anima 优质提示词（杂志封面贴纸网点海报）
